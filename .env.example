# Copy to .env.local (preferred) or .env — never commit secrets.

OPENAI_API_KEY=sk-...

# Model (optional). Supported names: OPENAI_MODEL or OPENAI_MODEL_ID
OPENAI_MODEL=gpt-4o-mini

# Organization (optional). Supported names: OPENAI_ORGANIZATION or OPENAI_ORG_ID
# OPENAI_ORG_ID=org-...
